{
  "name": "moxie-auction",
  "version": "1.0.0",
  "main": "api/auctions.js",  // Update to point to the main serverless function file
  "scripts": {
    "build": "tsc",  // Compiles TypeScript to JavaScript (ensure that your TypeScript files are set up correctly)
    "start": "node dist/api/auctions.js",  // Run the transpiled file (if using TypeScript)
    "dev": "ts-node src/auctions.ts"  // For development, run the TypeScript file directly
  },
  "dependencies": {
    "@airstack/node": "^0.0.5",
    "node-fetch": "^2.6.1",
    "@vercel/node": "^2.9.6"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "@types/node-fetch": "^2.5.8",
    "typescript": "^4.2.3",
    "ts-node": "^10.9.1"
  }
}
